import React from 'react'

const Checkout = () => {
  return
}

export default Checkout


// query for all items in user's cart, checkout will empty cart + subtract from balance
// refetch
// push to page (thanks for your purchase!)